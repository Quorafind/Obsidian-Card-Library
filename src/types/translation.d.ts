interface Translation {
    basic?: {
        welcome: string;
    };
    settings?: {
        title: string;
        general: string,
        cards: string,
        theme: string,
        output: string,
    };
}
